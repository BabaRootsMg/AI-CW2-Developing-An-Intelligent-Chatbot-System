"""
config.py
---------
Stores configuration settings and constants used across the chatbot system.
Includes file paths, station mappings, API keys (if needed), and default parameters.
Centralizes changes for easy maintenance and updates.
"""
